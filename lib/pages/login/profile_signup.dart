import 'package:flutter/material.dart';

class ProfileSignup extends StatelessWidget {
  const ProfileSignup({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Profile Signup'),);
  }
}